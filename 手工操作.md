# www.xiaoyuanvc.com SEO 手工操作清单

> 操作人：Jason
> 生成日期：2026-02-08
> DNS 管理：阿里云

---

## 一、Google Search Console — 提交 Sitemap

### 1.1 添加网站资源

1. 打开 <https://search.google.com/search-console/about>，用 Google 账号登录
2. 点击左上角下拉菜单 →「添加资源」
3. 选择右侧「**网址前缀**」（不要选"网域"）
4. 输入：`https://www.xiaoyuanvc.com/`
5. 点击「继续」

### 1.2 验证所有权（DNS TXT 记录）

Google 会给出一条验证字符串，格式类似：

```
google-site-verification=AbCdEfGhIjKlMnOpQrStUvWxYz
```

在阿里云添加这条记录：

1. 登录 [阿里云 DNS 控制台](https://dns.console.aliyun.com/)
2. 找到 `xiaoyuanvc.com` → 点击「解析设置」
3. 点击「添加记录」：

| 字段 | 填写内容 |
|------|---------|
| 记录类型 | TXT |
| 主机记录 | @ |
| 记录值 | 粘贴 Google 给的完整字符串 |
| TTL | 默认 10 分钟 |

4. 保存后回到 Search Console，点击「验证」
5. 如果提示未生效，等 5-10 分钟后重试

### 1.3 提交 Sitemap

验证成功后：

1. 左侧菜单 →「**站点地图**」
2. 在输入框中填入：`https://www.xiaoyuanvc.com/sitemap.xml`
3. 点击「提交」
4. 状态显示「成功」即完成

---

## 二、百度搜索资源平台 — 提交 Sitemap

### 2.1 添加网站

1. 打开 <https://ziyuan.baidu.com/>，用百度账号登录
2. 顶部菜单「用户中心」→「站点管理」→「添加网站」
3. 协议选择 `https://`，输入 `www.xiaoyuanvc.com`
4. 站点属性选择：教育培训

### 2.2 验证所有权（DNS CNAME 记录）

百度会给出一条 CNAME 验证值，格式类似：

```
主机记录：xxxxxxxx
记录值：zz.baidu.com
```

在阿里云添加：

1. 进入 [阿里云 DNS 控制台](https://dns.console.aliyun.com/) → `xiaoyuanvc.com` → 解析设置
2. 点击「添加记录」：

| 字段 | 填写内容 |
|------|---------|
| 记录类型 | CNAME |
| 主机记录 | 百度提供的主机记录值 |
| 记录值 | 百度提供的 CNAME 地址（如 `zz.baidu.com`） |
| TTL | 默认 10 分钟 |

3. 保存后回到百度，点击「完成验证」

### 2.3 提交 Sitemap

验证成功后：

1. 左侧菜单「普通收录」→「资源提交」→ 选择「sitemap」
2. 输入：`https://www.xiaoyuanvc.com/sitemap.xml`
3. 提交

---

## 三、HTTPS 跳转一致性

**目标：** 所有变体都 301 跳转到 `https://www.xiaoyuanvc.com`

```
http://xiaoyuanvc.com         → 301 → https://www.xiaoyuanvc.com
http://www.xiaoyuanvc.com     → 301 → https://www.xiaoyuanvc.com
https://xiaoyuanvc.com        → 301 → https://www.xiaoyuanvc.com
```

### 方案 A：部署到 GitHub Pages（推荐）

如果新官网部署到 GitHub Pages，跳转由 GitHub 自动处理：

**第一步：DNS 配置**（阿里云控制台）

在 `xiaoyuanvc.com` 的解析设置中，确保以下记录存在：

| 记录类型 | 主机记录 | 记录值 |
|---------|---------|--------|
| CNAME | www | `你的GitHub用户名.github.io` |
| A | @ | `185.199.108.153` |
| A | @ | `185.199.109.153` |
| A | @ | `185.199.110.153` |
| A | @ | `185.199.111.153` |

> 如果 `www` 已有旧的 A 记录或 CNAME 记录，先删除再添加新的。

**第二步：GitHub 仓库配置**

1. 进入仓库 Settings → Pages
2. Custom domain 填入：`www.xiaoyuanvc.com`
3. 等待 DNS 检查通过（可能需要几分钟）
4. 勾选「**Enforce HTTPS**」

完成后 GitHub 会自动处理：裸域 → www、HTTP → HTTPS 的 301 跳转。

### 方案 B：自有服务器（Nginx）

如果继续用自有服务器，在 Nginx 配置中添加：

```nginx
# HTTP → HTTPS 跳转（裸域 + www）
server {
    listen 80;
    server_name xiaoyuanvc.com www.xiaoyuanvc.com;
    return 301 https://www.xiaoyuanvc.com$request_uri;
}

# HTTPS 裸域 → www 跳转
server {
    listen 443 ssl;
    server_name xiaoyuanvc.com;
    ssl_certificate     /path/to/cert.pem;
    ssl_certificate_key /path/to/key.pem;
    return 301 https://www.xiaoyuanvc.com$request_uri;
}
```

修改后执行：

```bash
sudo nginx -t          # 检查配置语法
sudo systemctl reload nginx   # 重载生效
```

---

## 验证清单

全部操作完成后，逐项检查：

- [ ] Google Search Console 验证通过，sitemap 状态为「成功」
- [ ] 百度搜索资源平台验证通过，sitemap 已提交
- [ ] 访问 `http://xiaoyuanvc.com` → 跳转到 `https://www.xiaoyuanvc.com` ✓
- [ ] 访问 `http://www.xiaoyuanvc.com` → 跳转到 `https://www.xiaoyuanvc.com` ✓
- [ ] 访问 `https://xiaoyuanvc.com` → 跳转到 `https://www.xiaoyuanvc.com` ✓

可用以下命令快速验证跳转：

```bash
curl -I http://xiaoyuanvc.com
curl -I http://www.xiaoyuanvc.com
curl -I https://xiaoyuanvc.com
```

三条命令都应返回 `301` 和 `Location: https://www.xiaoyuanvc.com/`。
